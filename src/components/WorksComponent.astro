---
import '../styles/global.css';
import { content, defaultLanguage } from "../i18n/content";

const worksContent = content[defaultLanguage].works;
---

<section data-i18n-works>
    <h1>{worksContent.heading}</h1>
    {worksContent.sections.map((section) => (
        <div class="mt-6">
            <h2>{section.title}</h2>
            {section.items.map((item) => (
                <article class="mt-4 space-y-2">
                    <h3>{item.name}</h3>
                    {item.image && (
                        <img
                            src={item.image.src}
                            alt={item.image.alt}
                            width={item.image.width}
                            height={item.image.height}
                        />
                    )}
                    {item.links?.map((link) => (
                        <p>
                            {link.label}:{" "}
                            <a href={link.href} target="_blank" rel="noopener noreferrer">
                                {link.text}
                            </a>
                        </p>
                    ))}
                    <p>{item.description}</p>
                    {item.details && <p>{item.details}</p>}
                    {item.tech && <p>{item.tech}</p>}
                </article>
            ))}
        </div>
    ))}
</section>
